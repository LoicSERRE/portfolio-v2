/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/portfolio',
  images: {
    domains: ['upload.wikimedia.org', 'cdn.icon-icons.com', 'logowik.com', 'global.discourse-cdn.com'],
  },
};

export default nextConfig;